# Wails build artifacts
.task
bin
frontend/dist
frontend/node_modules
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Icon build intermediates — the committed build/darwin/icons.icns is the
# source of truth; the iconset staging dir and SVG sources here are
# local-only working files.
build/icon.iconset/
build/icon/

# Claude Code working files — plans and local settings are per-user, not
# repo state.
.claude/plans/
.claude/settings.local.json
.spec-mix/
