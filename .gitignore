# Wails build artifacts
.task
bin
frontend/dist
frontend/node_modules
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Icon build intermediates — the iconset staging dir is regenerated from
# the SVG source by `iconutil`. The SVG + final icns in build/icon/ are
# committed as source assets; the iconset is a throwaway.
build/icon.iconset/

# Claude Code working files — plans and local settings are per-user, not
# repo state.
.claude/plans/
.claude/settings.local.json
.spec-mix/
